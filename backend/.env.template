# Lokale Auth-Secrets
JWT_SECRET=change_me_to_a_long_random_string
PORT=3001

# Vercel KV (Upstash Redis) — nach dem Verlinken des KV-Stores in Vercel:
# Project → Storage → KV Store → ".env.local" Tab kopieren, oder lokal:
#   vercel env pull backend/.env
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
KV_URL=
